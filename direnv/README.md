# direnv

`direnv.toml`, copied to `~/.config/direnv/` by `sync-config`. Only errors are printed.

| Command        | Does                              |
| -------------- | --------------------------------- |
| `direnv allow` | trust the `.envrc` of this folder |
| `pyvenv`       | create `.venv` and its `.envrc`   |
