# wezterm

WezTerm terminal config (`.wezterm.lua`). Synced to `~/.wezterm.lua` by `sync.sh`.

## Settings

| Option | Value |
|--------|-------|
| `font` | JetBrainsMono NF |
| `font_size` | 14.0 |

Everything else uses WezTerm defaults.
