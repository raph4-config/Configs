# claude

Claude Code config, copied into `~/.claude/` by `sync-config`.

| Path            | Does                                                          |
| --------------- | ------------------------------------------------------------- |
| `skills/`       | personal skills, mirrored to `~/.claude/skills/`              |
| `statusline.sh` | status line, enable once with `statusLine` in `settings.json` |

| Skill          | Does                                        |
| -------------- | ------------------------------------------- |
| `explain-diff` | explain a pull request and what to review   |
| `setup`        | write a CLAUDE.md and lock credential files |
| `ship`         | commit, push and open a pull request        |
